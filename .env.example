# Used at build-time for direct API calls (optional, uses /api fallback → nginx proxy)
VITE_API_URL=https://backend-production.up.railway.app/api

# Used at runtime by nginx to proxy /api/* requests (set both on Railway)
BACKEND_URL=https://backend-production.up.railway.app
